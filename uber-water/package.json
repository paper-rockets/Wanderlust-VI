{
  "name": "three-stylized-water",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5180",
    "build": "vite build && node tools/check-pages-build.mjs",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit && node --check tools/perf.mjs && node --check tools/check-pages-build.mjs",
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "perf": "node tools/perf.mjs"
  },
  "dependencies": {
    "lil-gui": "^0.21.0",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.4",
    "playwright-core": "^1.62.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.1"
  }
}
