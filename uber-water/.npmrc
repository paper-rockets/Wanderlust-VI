# Supply-chain hardening: do not run dependency lifecycle scripts implicitly.
ignore-scripts=true

# Allow time for newly published malicious packages to be detected and removed.
min-release-age=7

# Fail installation when the npm version cannot enforce the policy above.
engine-strict=true
